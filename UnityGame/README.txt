git clone to add new project

cd into pathway

git pull to grab someone else's push

git status shows you things you can add

git add then pathway

git commit shows you things that you're finalizing
git commit -m "[COMMENT]"

git push to send it